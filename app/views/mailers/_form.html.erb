<%= form_with(model: mailer) do |form| %>
  <% if mailer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mailer.errors.count, "error") %> prohibited this mailer from being saved:</h2>

      <ul>
        <% mailer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
    <%= form.label :to, class: "form-label mb-2 mt-3" %>
    <%= form.text_area :to, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :subject, class: "form-label mb-2 mt-3" %>
    <%= form.text_field :subject, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :body, class: "form-label mb-2 mt-3" %>
    <%= form.text_area :body, class: "form-control" %>
  </div>

  <div>
    <%= form.submit  %>
  </div>
<% end %>
