<%= form_with(model: pationt) do |form| %>

  <div class="form-group">
    <%= form.label :name, style: "display: block", class: "form-label mb-2 mt-3" %>
    <%= form.text_field :name, class: "form-control form-control-lg" %>
  </div>

  <div class="form-group">
    <%= form.label :last_name, style: "display: block", class: "form-label mb-2 mt-3" %>
    <%= form.text_field :last_name, class: "form-control form-control-lg" %>
  </div>

  <!-- <%= form.fields_for :physicians do |physician_form| %>
    <div class="form-group">
      <%= physician_form.label :name, "Physician Name", class: "form-label mb-2 mt-3" %>
      <%= physician_form.text_field :name, class: "form-control form-control-lg" %>
    </div>
  <% end %> -->

  <div class="form-group">
    <%= form.submit %>
  </div>
<% end %>
