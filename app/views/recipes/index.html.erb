<p style="color: green"><%= notice %></p>
<table class="table">
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Dish Type</th>
      <th scope="col">Edit</th>
      <th scope="col">Show</th>
      <th scope="col">Destroy</th>
    </tr>
  </thead>
  <tbody>
    <% @recipes.each do |recipe| %>
      <tr>
        <td><%= recipe.name %></td>
        <td><%= recipe.dish_type %></td>
        <td><%= link_to "Edit", edit_recipe_path(recipe) %></td>
        <td><%= link_to "Show", recipe_path(recipe) %></td>
        <td><%= link_to "Destroy", recipe, data: { turbo_confirm: "Are you sure you want to delete this user", turbo_method: :delete } %></td>
      </tr>
    <% end %>  
  </tbody>
</table>

<%= link_to "New recipe", new_recipe_path %>



<!-- 
<div data-controller="practice">
  <input data-practice-target="name" type="text">
  <button data-action="click->practice#greet">Greet</button>
</div>

<p id="demo"></p>

<div data-controller="clipboard">
 PIN1: <input data-clipboard-target="source" type="text" value="1234" readonly>
 <button data-action="clipboard#copy">Copy to Clipboard</button>
</div>

<div data-controller="clipboard">
  PIN2: <input data-clipboard-target="source" type="text" value="3737" readonly>
  <button data-action="clipboard#copy">Copy to Clipboard</button>
</div>

<div data-controller="clipboard">
  PIN3: <input data-clipboard-target="source" type="text" value="37337" readonly>
  <a href="#" data-action="clipboard#copy">Copy to Clipboard</a>
</div>

 PIN4: <textarea data-clipboard-target="source" readonly>3737</textarea>

  <div data-controller="clipboard" data-clipboard-supported-class="clipboard--supported">
    <button data-action="clipboard#copy" class="clipboard-button">Copy to Clipboard</button>

    <style>
      .clipboard-button {
       display: none;
      }

      .clipboard--supported .clipboard-button {
        display: initial;
      }
    </style> -->